<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nidhogg.dao.CheckItemDao">
    <!--插入检查项数据-->
    <insert id="add" parameterType="com.nidhogg.pojo.CheckItem">
        insert into t_checkitem(code,name,sex,age,price,type,remark,attention)
                      values
        (#{code},#{name},#{sex},#{age},#{price},#{type},#{remark},#{attention})
    </insert>
    <!--按条件查询检查项-->
    <select id="selectByCondition" parameterType="string"
            resultType="com.nidhogg.pojo.CheckItem">
        select * from t_checkitem
        <if test="value != null and value.length > 0">
            where code = #{value} or name like CONCAT(CONCAT('%', #{value}), '%')
        </if>
    </select>
    <!--删除检查项-->
    <delete id="deleteById" parameterType="int">
        delete from t_checkitem where id = #{id}
    </delete>
    <!--根据检查项id查询中间关系表-->
    <select id="findCountByCheckItemId" resultType="long" parameterType="int">
        select count(*) from t_checkgroup_checkitem where checkitem_id = #{checkitem_id}
    </select>
    <!--编辑检查项-->
    <update id="edit" parameterType="com.nidhogg.pojo.CheckItem">
        update t_checkitem
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="code != null">
                code = #{code},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="attention != null">
                attention = #{attention},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
        where id = #{id}
    </update>
    <!--根据id查找检查项-->
    <select id="findById" resultType="com.nidhogg.pojo.CheckItem" parameterType="int">
        select * from t_checkitem where id=#{id}
    </select>
    <!--查找所有检查项-->
    <select id="findAll" resultType="com.nidhogg.pojo.CheckItem">
        select * from t_checkitem
    </select>
    <!--根据检查组id查询检查项信息-->
    <select id="findCheckItemById" resultType="com.nidhogg.pojo.CheckItem">
        select * from t_checkitem
        where id
        in (select checkitem_id from t_checkgroup_checkitem where checkgroup_id=#{id})
    </select>
</mapper>